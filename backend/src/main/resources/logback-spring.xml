<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- コンソール出力用アペンダー -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- アプリケーションログ用ファイルアペンダー -->
    <appender name="APPLICATION_LOG" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/application.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/application.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>100MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- セキュリティログ専用ファイルアペンダー -->
    <appender name="SECURITY_LOG" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/security.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [SECURITY] %-5level - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/security.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>90</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- 監査ログ専用ファイルアペンダー -->
    <appender name="AUDIT_LOG" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/audit.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [AUDIT] %-5level - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/audit.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>365</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- セキュリティログ専用ロガー -->
    <logger name="SECURITY" level="INFO" additivity="false">
        <appender-ref ref="SECURITY_LOG"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- 監査ログ専用ロガー -->
    <logger name="AUDIT" level="INFO" additivity="false">
        <appender-ref ref="AUDIT_LOG"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- SpringSecurityのログレベル設定 -->
    <logger name="org.springframework.security" level="DEBUG"/>
    
    <!-- セキュリティ例外ハンドラーのログレベル設定 -->
    <logger name="com.library.management.exception.SecurityExceptionHandler" level="INFO"/>

    <!-- ルートロガー -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="APPLICATION_LOG"/>
    </root>

    <!-- プロファイル別設定 -->
    <springProfile name="dev">
        <root level="DEBUG">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="prod">
        <root level="WARN">
            <appender-ref ref="APPLICATION_LOG"/>
        </root>
        
        <!-- 本番環境では機密ログを別途暗号化して保存することを推奨 -->
        <logger name="SECURITY" level="INFO" additivity="false">
            <appender-ref ref="SECURITY_LOG"/>
        </logger>
        
        <logger name="AUDIT" level="INFO" additivity="false">
            <appender-ref ref="AUDIT_LOG"/>
        </logger>
    </springProfile>
</configuration>